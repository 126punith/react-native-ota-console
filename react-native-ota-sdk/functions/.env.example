# Optional: Override APK path prefix (default: apks/default/)
# Only process files uploaded under this path, e.g. apks/myapp/v2.apk
APK_PATH_PREFIX=apks/myapp/

# Optional: Override function region (default: us-central1)
# Should be close to your Firebase Storage bucket for lower latency
REGION=us-central1
